<!DOCTYPE html>
<html>
<head>
  <title>Healthy Desserts</title>
  <%= stylesheet_link_tag  "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= controller.controller_name %>">
<div id="banner">
  <%= @page_title || "Healthy Desserts" %>
  <div id="current_user">
    <% if session[:user_id] %>
      <p>Welcome, <%= User.find_by_id(session[:user_id]).name %></p>
    <% end %>
  </div>
</div>

 <div id="columns">
    <div id="side">
      <ul>
        <li><%= link_to 'All Recipes', root_url %></li>
        <li><%= link_to 'Add A Recipe', new_recipe_path %>
        <% if session[:user_id] %> <!-- If valid user -->
        <li><%= link_to 'Your Recipes', recipes_path %>
        <li><%= link_to 'Log Out', '/logout', :method=>:delete %></li>
        <% end %>
        <% if !session[:user_id] %> <!-- If not valid user -->
        <li><%= link_to 'Log In', '/login' %></li>
        <% end %>
      </ul>
    </div>
    <div id="main">

<!-- END_HIGHLIGHT -->
      <%= yield %><!-- <label id="code.depot.e.include"/> -->
<!-- START_HIGHLIGHT -->
    </div>
  </div>
  <div id="footer">
    <p>© 2012.  All rights reserved.</p>
  </div>
<!-- END_HIGHLIGHT -->
</body>
</html>